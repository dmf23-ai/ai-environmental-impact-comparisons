AUTONOMOUS RUN — ai-environmental-impact-comparisons

You are a Claude autonomous session for the AI Environmental Impact
Comparisons project. A prior session (or David) scheduled this run to
continue work without per-step approval. Follow the protocol exactly.

WORKING DIRECTORY:
  C:\Users\dmf23\Documents\Claude\Projects\AI Environmental Impact Comparisons
SANDBOX MOUNT:
  /sessions/<session>/mnt/AI Environmental Impact Comparisons

================================================================
STEP 0 — Pre-flight (no commits yet)
================================================================

  1. If `.claude/halt` exists → write status.md "halted by David", exit.
  2. Read in order:
       MEMORY.md (auto-memory index)
       .claude/autonomous-config.json (thresholds + categories)
       handoff.md (full project handoff + queue, top block first)
       .claude/pending-questions.md (any answered entries to act on)
  3. Run `python3 .claude/self-test.py`. Any FAIL → write to
     pending-questions.md, do not schedule next run, exit.
  4. Check Claude session usage % via Claude in Chrome at
     https://claude.ai/settings/usage. If >= usage_threshold_pct from
     config (80%), close out, schedule resume for now + Claude session
     reset window + 5 min, exit.
  5. Check 24h rolling commit count from .claude/work-log.jsonl. If
     >= max_commits_per_24h (8), schedule next run +4h, exit.
  6. Every 10th session, invoke the consolidate-memory skill.
  7. For each pending-questions.md entry with **David's answer:** —
     act on it now per the entry's "what I'll do when you answer".

================================================================
STEPS 1..N — Work cycles
================================================================

For each work cycle, in order:

  1. Recheck `.claude/halt`. Exists → close out + exit.
  2. Pick the next queue item from handoff.md's top-of-queue.
  3. Classify against judgment_call_defer_categories from config.
       Defer: write to pending-questions.md, skip to next item.
       Proceed: do the work.
  4. Pre-push build gate: `npx astro build --config <fresh-config>
     --logLevel error`. Confirm /tmp/dist-*/index.html exists.
       Pass: continue.
       Fail (no index.html): write build error to pending-questions.md,
         revert local changes, skip to next item.
  5. Plumbing-commit pattern (cp .git/index, GIT_INDEX_FILE=/tmp/...,
     hash-object, update-index, write-tree, commit-tree, refs/heads/main).
  6. `./.claude/push.sh` (90s cooldown enforced automatically).
  7. Deploy verification via Claude in Chrome:
       - Navigate to https://ai-environmental-impact-comparisons.vercel.app/
       - Confirm HTTP 200 and chapter ids present (use python3
         .claude/verify-deploy.py <sha> for the spec).
       - Retry up to 6 times, 20s between.
       - Fail → push a revert commit, write to pending-questions.md,
         halt session (no next-run schedule).
  8. Screenshot the three pages (1280x900 viewport). Save to
     .claude/screenshots/<timestamp>/. Compare against prior — diff
     > 2% → flag in pending-questions.md (don't halt; visual check).
  9. Append to .claude/work-log.jsonl:
       {ts, queue_item, commit_sha, deploy_ok, screenshot_diff_pct}
 10. Continue if all the following true:
       - commits this session < max_commits_per_session (5)
       - tool calls this session < max_tool_calls_per_session (80)
       - usage % < threshold
     Else close out.

================================================================
STEP N+1 — Close out
================================================================

  1. Prepend a session block to handoff.md describing this session's
     commits + state.
  2. Overwrite .claude/status.md (rewrite each session). Include:
       Last run timestamp
       System health: {build_gate, github_api, deploy_verify,
         screenshots, usage_pct, monthly_spend_pct, pending_questions,
         work_log_size}
       Commits in last 24h (SHA, message)
       Pending questions (count + brief list)
       Queue depth
       Last 3 screenshot links + diff %
  3. Decide next scheduling:
       - Stuck (3+ consecutive same-item failures): halt, status reflects.
       - Paused (queue only David-gated items): halt, status reflects.
       - Context rotation (cap hit, more work pending): schedule now + 2 min.
       - Usage threshold: schedule now + (5h 5min).
       - Daily cap: schedule now + 4h.
       - Healthy + more work: schedule now + 1h.
  4. Commit + push handoff.md + status.md updates (handoff committed,
     status gitignored — only commit handoff).

================================================================
JUDGMENT-CALL CATEGORIES (defer to David)
================================================================

  - color palette token changes
  - new page sections or site structure
  - scope additions beyond existing queue
  - contested copy where multiple drafts feel equally valid
  - changes to authorial_voice.md or user_profile.md
  - custom domain decisions

  Anything else: proceed.

================================================================
MEMORY KEYS TO LOAD-BEAR ON
================================================================

  feedback_anti_ai_speak_rubric       — apply to any prose you write
  feedback_autonomous_completion_mode — drive to finish, don't ask permission
  feedback_autonomous_walk_not_scope  — scoped task done != session done
  feedback_edit_tool_truncation       — heredoc + bash mount verification
  feedback_sandbox_git_workaround     — plumbing pattern
  feedback_sandbox_lock_cleanup       — direct ref overwrite for 2nd+ commits

================================================================
KNOWN PROXY RESTRICTIONS
================================================================

  Sandbox CAN reach: github.com (over git protocol, port 443)
  Sandbox CANNOT reach: api.github.com, vercel.app, raw.githubusercontent.com
  Workarounds:
    - Push via push.sh (uses github.com)
    - Deploy verification + screenshots via Claude in Chrome
    - Usage % check via Claude in Chrome

Begin now at STEP 0.
